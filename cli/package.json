{
  "name": "nexioai",
  "version": "0.3.1",
  "description": "Nexio CLI — orchestrate AI agent teams to run a business",
  "type": "module",
  "bin": {
    "nexioai": "./dist/index.js"
  },
  "keywords": [
    "nexio",
    "ai",
    "agents",
    "orchestration",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/paperclipai/paperclip.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/paperclipai/paperclip",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node --input-type=module -e \"import esbuild from 'esbuild'; import config from './esbuild.config.mjs'; await esbuild.build(config);\" && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@nexioai/adapter-claude-local": "workspace:*",
    "@nexioai/adapter-codex-local": "workspace:*",
    "@nexioai/adapter-cursor-local": "workspace:*",
    "@nexioai/adapter-gemini-local": "workspace:*",
    "@nexioai/adapter-opencode-local": "workspace:*",
    "@nexioai/adapter-pi-local": "workspace:*",
    "@nexioai/adapter-openclaw-gateway": "workspace:*",
    "@nexioai/adapter-utils": "workspace:*",
    "@nexioai/db": "workspace:*",
    "@nexioai/server": "workspace:*",
    "@nexioai/shared": "workspace:*",
    "drizzle-orm": "0.38.4",
    "dotenv": "^17.0.1",
    "commander": "^13.1.0",
    "embedded-postgres": "^18.1.0-beta.16",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
